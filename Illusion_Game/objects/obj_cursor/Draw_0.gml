var mx = device_mouse_x(0); // Récupère la position X de la souris
var my = device_mouse_y(0); // Récupère la position Y de la souris
draw_sprite(spr_cursor, 0, mx, my); // Dessine le sprite du curseur à la position de la souris
